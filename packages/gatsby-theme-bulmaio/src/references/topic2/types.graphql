type Topic2 implements Node & Reference {
    id: ID!
    label: String!
    slug: String!
    title: String!
    subtitle: String
    body: String! @parentBody
    parent: Node
    resources: [Resource] @link(by: "topics2.label", from: "label")
}
