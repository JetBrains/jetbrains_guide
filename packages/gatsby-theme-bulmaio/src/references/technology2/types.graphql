type Technology2 implements Node & Reference {
    id: ID!
    label: String!
    slug: String!
    title: String!
    body: String! @parentBody
    parent: Node
    resources: [Resource] @link(by: "technologies2.label", from: "label")
}
