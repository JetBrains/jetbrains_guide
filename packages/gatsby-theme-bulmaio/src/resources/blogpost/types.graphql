type BlogPost implements Node & Resource {
    id: ID!
    slug: String!
    title: String!
    body: String! @parentBody
    parent: Node
    author: Author @link(by: "label")
    topics2: [Topic2] @link(by: "label")
    poster: String @defaultString(value: "XXXYYY")
}
