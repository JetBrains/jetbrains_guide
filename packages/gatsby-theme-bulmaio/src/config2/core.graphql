interface Resource @nodeInterface {
    author2: Author2
    body: String!
    id: ID!
    parent: Node
    slug: String!
    title: String!
    topics2: [Topic2]
}

interface Reference @nodeInterface {
    id: ID!
    label: String!
    slug: String!
    title: String!
    body: String!
    parent: Node
    resources: [Resource]
}
